# Native ChatGPT Codex provider profile
# Uses Goose chatgpt_codex provider auth. The provider uses the base GPT-5.5
# model ID; configure reasoning effort in Goose config when needed.

tiers:
  high: gpt-5.5
  mid: gpt-5.5
  low: gpt-5.5

providers:
  high: chatgpt_codex
  mid: chatgpt_codex
  low: chatgpt_codex
