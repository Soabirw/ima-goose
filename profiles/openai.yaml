# OpenAI via codex-acp profile
# Uses ChatGPT Pro/Max subscription auth (codex-acp provider).
# See docs/MODEL-TIERS.md for the full rationale.

tiers:
  opus: gpt-5.5/high # Flagship reasoning — orchestration, code review, planning
  sonnet: gpt-5.5/medium # Everyday coding — implementation and verification
  haiku: gpt-5.5/low # Fast sub-agent — exploration and lookups

# Per-recipe overrides applied AFTER tier mapping.
# OpenAI tiering is expressed as gpt-5.5 reasoning effort, so no model demotions
# are needed for heavy-fan-out recipes.
overrides: {}
