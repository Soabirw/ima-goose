# OpenAI via codex-acp profile (default)
# Uses ChatGPT Pro/Max subscription auth.

tiers:
  high: gpt-5.5/high # Deep reasoning — planning, review, research, orchestration
  mid: gpt-5.5/medium # Everyday implementation and release workflows
  low: gpt-5.5/low # Fast read-only exploration and lookups

providers:
  high: codex-acp
  mid: codex-acp
  low: codex-acp
