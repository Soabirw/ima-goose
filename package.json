{
  "name": "ima-goose",
  "version": "2.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "install:recipes": "node scripts/install.ts",
    "install:recipes:validate": "node scripts/install.ts --validate",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "eta": "^4.0.1"
  },
  "engines": {
    "node": ">=24"
  }
}
